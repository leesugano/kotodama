/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "kotodama",
  "compatibility_date": "2025-09-02",
  "compatibility_flags": ["nodejs_compat"],
  "main": "@tanstack/react-start/server-entry",
  "observability": {
    "enabled": true
  },
  "upload_source_maps": true,
  /**
   * Canonical domain. workers.dev stays active for old links and PWAs,
   * but the app redirects here on the client (__root.tsx).
   */
  "workers_dev": true,
  "routes": [
    {
      "pattern": "kotodama.leesugano.com",
      "custom_domain": true
    }
  ],
  "vars": {
    "BETTER_AUTH_URL": "https://kotodama.leesugano.com"
  },
  /**
   * D1: Better Auth users and sessions.
   * In v2 the same database receives script sync.
   */
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "kotodama",
      "database_id": "e8b82d22-3635-4a09-897b-64b671665392",
      "migrations_dir": "./migrations"
    }
  ]
  /**
   * KV for v2 (share a script by link, TTL):
   */
  // "kv_namespaces": [
  //   { "binding": "SHARES", "id": "<NAMESPACE_ID>" }
  // ]
}
